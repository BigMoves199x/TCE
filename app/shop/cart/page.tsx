import CartPageContent from "@/app/components/ui/shop/CartPageContent";

export const metadata = {
  title: "Shopping Cart | TCE",
};

export default function CartPage() {
  return <CartPageContent />;
}